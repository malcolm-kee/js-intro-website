/* eslint-disable */

// declare a variable x without value and then assign it with a value 100.
let x;
x = 100;

// declare a variable y with value true then replace the value with false.
let y = true;
y = false;

// declare a variable that store your first name, last name, and age.
const me = {
  firstName: 'Mickey',
  lastName: 'Mouse',
  age: 50
};
